<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @figures.each do |figure| %>
      <%=figure.name %>
    <% figure.titles.each do |title|%>
      <%= title.name %>
    <%end%>
      <% figure.landmarks.each do |landmark|%>
      <%= landmark.name %>
      <%end%>
  <%end%>
  </body>
</html>
